/**
 * The listing of art files actually present in `public/`, produced by the
 * `available-art` Vite plugin. See `vite.config.ts`.
 */
declare module 'virtual:available-art' {
  export const AVAILABLE_ART: ReadonlySet<string>;
}
